<?php

require_once 'database.php';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    //fichier obsolète (faute de temps)

    /*
    L'idée était de d'abord vérifier si le mot de passe et l'identifiant étaient corrects.
    Ensuite, un pop-up en JavaScript s'affichait, demandant de saisir le code pin si les informations précédemment saisies étaient correctes.
    Enfin, le code de ce fichier vérifiait la validité du code pin et renvoyait vers la bonne page home.php .
    */
}